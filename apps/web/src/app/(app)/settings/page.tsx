export default function SettingsPage() {
    return (
        <div className="space-y-6 animate-[fade-in_0.3s_ease-out]">
            <div>
                <h1 className="heading-1">Definições</h1>
                <p className="body-sm mt-1" style={{ color: 'var(--text-secondary)' }}>Preferências, exportações e upload</p>
            </div>
            <div className="card text-center" style={{ padding: 'var(--space-3xl) var(--space-lg)' }}>
                <p style={{ color: 'var(--text-secondary)' }}>Definições — em desenvolvimento</p>
            </div>
        </div>
    );
}
